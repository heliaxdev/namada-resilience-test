name: Rebuild Namada images

on:
  workflow_dispatch:
    inputs:
      commit:
        description: "The namada SHA commit to build"
        required: true
        type: string
      genesis_template_version:
        description: "The genesis template version"
        required: true
        type: string
  schedule:
    - cron: "0 20 * * *"
  pull_request:
    paths:
      - 'config/**'
      - 'namada/**'
      - 'genesis/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

jobs:
  namada:
    runs-on: ubuntu-latest
    env:
      antithesis_registry_url: us-central1-docker.pkg.dev/molten-verve-216720/heliax-repository
    strategy:
      fail-fast: true
      matrix:
        images:
          - context: "config"
            image: "namada-config"
            tag: ""
            cache_scope: "config-image"
          - context: "genesis"
            image: "namada-genesis"
            tag: ""
            cache_scope: "genesis-image"
          - image: "namada"
            tag: ""
            context: "namada"
            cache_scope: "namada-image"
          - image: "namada"
            tag: -inst
            context: "namada"
            cache_scope: "namada-inst-image"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to private registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.antithesis_registry_url }}
          username: _json_key
          password: ${{ secrets.ANTITHESIS_GAR_JSON_KEY }}
      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & Push Image ${{ matrix.images.image }} with sha commit main/'0.46.0-ant' (scheduled)
        if: ${{ github.event_name == 'schedule' }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.images.context }}
          push: true
          tags: |
            ghcr.io/heliaxdev/ant-${{ matrix.images.image }}:main${{ matrix.images.tag }}
            us-central1-docker.pkg.dev/molten-verve-216720/heliax-repository/${{ matrix.images.image }}:main${{ matrix.images.tag }}
          cache-from: type=gha,scope=${{ matrix.images.cache_scope }}
          cache-to: type=gha,scope=${{ matrix.images.cache_scope }},mode=max
          build-args: |
            GENESIS_TEMPLATE_VERSION=0.46.0-ant
            GIT_SHA=main
      - name: Build & Push Image ${{ matrix.images.image }} with sha commit ${{ github.ref_name || github.head_ref }}/0.46.0-ant (pull request)
        if: ${{ github.event_name == 'pull_request' }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.images.context }}
          push: false
          tags: |
            ghcr.io/heliaxdev/ant-${{ matrix.images.image }}:${{ github.ref_name || github.head_ref }}${{ matrix.images.tag }}
            us-central1-docker.pkg.dev/molten-verve-216720/heliax-repository/${{ matrix.images.image }}:${{ github.ref_name || github.head_ref }}${{ matrix.images.tag }}
          cache-from: type=gha,scope=${{ matrix.images.cache_scope }}
          cache-to: type=gha,scope=${{ matrix.images.cache_scope }},mode=max
          build-args: |
            GENESIS_TEMPLATE_VERSION=0.46.0-ant
            GIT_SHA=${{ github.ref_name || github.head_ref }}
      - name: Build & Push Image ${{ matrix.images.image }} with sha commit ${{ github.event.inputs.commit }}/${{ github.event.inputs.genesis_template_version }} (workflow dispatch)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.images.context }}
          push: true
          tags: |
            ghcr.io/heliaxdev/ant-${{ matrix.images.image }}:${{ github.event.inputs.commit }}${{ matrix.images.tag }}
            us-central1-docker.pkg.dev/molten-verve-216720/heliax-repository/${{ matrix.images.image }}:${{ github.event.inputs.commit }}${{ matrix.images.tag }}
          cache-from: type=gha,scope=${{ matrix.images.cache_scope }}
          cache-to: type=gha,scope=${{ matrix.images.cache_scope }},mode=max
          build-args: |
            GENESIS_TEMPLATE_VERSION=${{ github.event.inputs.genesis_template_version }}
            GIT_SHA=${{ github.event.inputs.commit }}
